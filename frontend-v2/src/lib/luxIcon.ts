/**
 * Iconografía de interfaz: grosor de trazo y clase base alineados con el lujo
 * (sombras, hover) — importar con los iconos Lucide (`strokeWidth` + `className`).
 */
export const LUX_ICON_STROKE = 1.45 as const

/** Cabecera / chrome (menú, usuarios, hash). */
export const luxIconHeader = 'lux-icon h-5 w-5 shrink-0 text-muted-foreground/85'

/** Botones y filas (canales, acciones en lista). */
export const luxIconRow = 'lux-icon h-4 w-4 shrink-0 text-muted-foreground/80'

/** Iconos de mensaje (hover group ya en MessageItem). */
export const luxIconMessage = 'lux-icon h-4 w-4'

/** Icono compacto (pies, avisos). */
export const luxIconSm = 'lux-icon h-3.5 w-3.5'
